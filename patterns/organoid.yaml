pattern_name: organoid/patterns/organoid
classes:
  organoid: ORGANOID:0000001

relations:
  is model of: RO:0003301
    
vars: 
  models: "'Thing'"

name:
  text: "%s organoid"
  vars:
    - models

annotations: 
  - property: oio:hasExactSynonym
    text: "model of %s"
    vars:
      - models

def: 
  text: "Any organoid that models a %s."
  vars:
    - models
    
equivalentTo: 
  text: "'organoid' and 'is model of' some %s"
  vars:
    - models

